/*input
aba
*/
#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main()
{
	ios_base::sync_with_stdio(false);
    cin.tie(NULL);

	int n;
	string s;
	cin>>s;
	cout<<s;
	reverse(s.begin(),s.end());
	cout<<s;
	return 0;
}